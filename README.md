# Base64 Tools Backup

[Download the backup Base64 bundle](./PLMXML_ALL_MODULES_NOCOMMENTS_BASE64.txt)

Backup of **PLMXML_ALL_MODULES_NOCOMMENTS_BASE64.txt**, preserved exactly as supplied. This is one complete Base64 file. Its original filename, encoding, line breaks, and module markers are unchanged.

## Download and use

1. Open the bundle above and choose **Download raw file**.
2. Decode the complete file from Base64, allowing whitespace and line breaks.
3. Use an importer compatible with this backup's original module markers. The decoded file uses `#MODULE ModuleName` and `#END ModuleName`.

This is an archived source bundle. Publication verifies the file's integrity and Base64 decoding; it does not establish that this backup's VBA compiles or behaves like the newer release.

## File verification

- Original file size: **1,674,852 bytes**.
- Decoded size: **1,223,931 bytes**.
- SHA-256: `8fa7c334b44df8500d504a9bf415296b7c80a18447cf43dcb9961756ea47ee51`.
